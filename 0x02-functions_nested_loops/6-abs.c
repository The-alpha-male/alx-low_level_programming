#include "main.h"

/**
 *_ abs -computes the absolute value of an integer
 * @n - interger to be computed.
 *  Return: absolute integer value
 */

int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
